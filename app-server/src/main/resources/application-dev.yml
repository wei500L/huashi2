spring:
  config:
    activate:
      on-profile: dev
  flyway:
    baseline-on-migrate: true

management:
  tracing:
    sampling:
      probability: 1.0
  endpoint:
    health:
      show-details: when_authorized
