spring:
  config:
    activate:
      on-profile: local
  flyway:
    baseline-on-migrate: true

management:
  tracing:
    sampling:
      probability: 1.0
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.huashi.eftransfer: DEBUG
