请根据以下上下文生成“诊断结果解释”。

输出要求：
- 只返回 JSON
- 明确解释 diagnosis summary、highRiskLexicalPairs、errorTypeDistribution、contextSensitivity、semanticDiscrimination 的含义
- 结论不能脱离上下文数据
- 必须返回 citationIds 和 uncertaintyNote

上下文：
{{CONTEXT_JSON}}
