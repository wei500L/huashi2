spring:
  config:
    activate:
      on-profile: local
  flyway:
    baseline-on-migrate: true

management:
  tracing:
    sampling:
      probability: 1.0

logging:
  level:
    com.huashi.eftransfer: DEBUG
